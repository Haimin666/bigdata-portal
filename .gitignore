# 本地凭证与敏感配置,不入库
datasources.json
.env.local
# 网关 JSON 配置(唯一配置源,含账号/令牌,不入库)
server/config.local.json
# docker compose 变量替换文件(内网构建参数等,按环境定制)
.env
.reasonix
# 依赖与构建
node_modules/
dist/

# 系统与编辑器
.DS_Store
*.log
.idea
.vscode

# npmctl 运行时产物(进程 PID 与日志)
.portal.pid
.portal.log

# playwright-cli 会话产物(快照/截图)
.playwright-cli/
data/
*.png
# 原型演示目录(不入库)
demo-proto/
# flink 测试脚本目录(含真实连接串,不入库)
flink-demo/
services/db-proxy/datasources.json
mysql_online_conns.json
tmp/

# doubao-chat 持久化浏览器 profile（含登录态 cookie，不入库）
.doubao_profile/
.doubao_chrome_profile/

# DSH 技能目录(工具安装产物,非项目源码)
.dsh/
# 本地 UI demo(可敲定参考,非交付物)
demo-query-ui/
# MCP 本地配置(含本机绝对路径)
.mcp.json
# Python 编译缓存
__pycache__/
*.pyc
