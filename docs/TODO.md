# TODO 清单（持续推进）

> 记录前端静态审查发现的问题。状态：`open` = 待做，`done` = 完成。
> 优先级 P1 最优先。

## P1 · 任务监控默认视图空态（"没有内容"主因）
- 文件：`src/views/ds/DsTaskMonitor.vue`
- 现象：打开"任务监控"页面列表空白，只显示空态引导"暂无工作流实例"。
- 根因链：`rangeKey` 默认 `'today'`（只查当天）+ `findDefaultProject` 只探测项目列表有限数量当天是否有实例，找不到回退 `list[0]`，而 `list[0]` 当天大概率也没实例。
- 触发场景：项目多 / 凌晨打开 / 当天活跃项目排在前 5 之外。
- 可选修法：探测范围扩大 / 改选"当天实例数最多"的项目 / 空态加引导文案（如"当天无实例,可切换时间范围"）。
- 状态：done（默认项目探测扩大并增加无数据引导）

## P2 · 任务视图「全部项目+搜索」传错参数（确定逻辑 bug）
- 文件：`src/views/ds/DsTaskMonitor.vue`，`loadAllOrOne` 精确分支（约 221-244 行）
- 根因：`h.matchedTask` 是命中的**任务名**（`src/api/dsDeps.ts` 中 `matchedTask: string`），但 task 视图查询时传 `taskName: t.processName`（工作流名）→ 后端按任务名匹配必然查不到 → 空结果。
- 正确应传 `h.matchedTask`。
- 触发场景：任务视图 + "全部项目" + 搜索。
- 状态：done（精确任务查询改用命中的任务名）

## P3 · QueryView 初始化只认 datasources，不认 allowedDbs
- 文件：`src/views/db/QueryView.vue`，`onMounted`（1963-1989 行）
- 现象：网关 acl 因角色过滤 `datasources=[]`（`allowedDbs` 有 300+）时，每次打开弹「未配置数据库源(检查网关 DB_PROXY_URL)」误导文案，且 `engine`/`db` 默认值不初始化；实际查询靠 `allowedDbs` 兜底仍可用。
- 可选修法：初始化兜底用 `allowedDbs`；文案改为"当前账号无可视数据源(网关权限过滤)"。
- 状态：done（初始化兼容 allowedDbs 并修正文案）

## P4 · DsDepsPanel 共享节点误标为上游
- 文件：`src/views/ds/DsDepsPanel.vue`，`toDagData()`
- 现象：同一 `processId` 同时出现在上游树和下游树时合并节点并永久标 `isUpstream=true` → 该节点失去"重跑"勾选入口。
- 触发场景：跨路径共享节点的工作流。
- 状态：done（共享节点按上下游关系保留可重跑能力）

## P5 · 性能提示（非 bug，可选优化）
- 文件：`src/views/ds/DsTaskMonitor.vue`，`resolveYarnAppId`
- 现象：展开工作流的**每个任务**逐个发日志接口请求（限 4 路并发），任务多时瞬时请求量大。
- 状态：done（同一实例的 YARN 日志解析增加缓存与按需加载）

## P1 · Vben 视觉统一
- 状态：in-progress
- 已完成：门户壳层、登录页、主布局、按钮基线、用户管理页、HDFS/OMD 视觉验证、移动端侧栏抽屉；数据库查询与任务监控暗色页面验证；任务监控 390px 筛选栏修复并复验。
- 2026-09-20 巡检：YARN、任务监控、HDFS、数据库查询暗色/窄屏(390/360px)通过页面级溢出检查；HDFS 窄屏路径工具栏与数据库查询底部状态栏已修复。OMD iframe 外框 360px 正常；跨源子应用内部主题/排版不在门户可观测范围。
- 2026-09-20 YARN 视觉实改：筛选/偏好工具栏分组、四张独立资源卡、表格改轻量横向分隔；1280px、390px及暗色截图复验，未见页面级横向溢出。
- 待完成：认证开启后的用户管理交互回归；数据同步模块未包含在当前 `enabledModules` 中，已补窄屏样式，但未绕过权限做页面级浏览器验证。开发助手受项目开发范围豁免约束，除非用户明确重新纳入，否则不做专项修改。
- Vben 目标页精确对照：`/system/role` 当前重定向到公开演示登录页并要求滑块验证；不自动绕过挑战。已从实际页面确认主色 `#006be6` 并统一门户品牌蓝，角色页布局仍需用户提供登录后的截图/录屏或可访问会话继续像素级核验。
- 环境待验证：本地 `/api/db/acl` 与 `/api/spark/status` 返回 502，需 db-proxy 可用后复测数据库源初始化与 Spark 状态；目前 UI 可渲染，但不能据此认定后端链路已验证。
- 构建体积：超大 chunk 主要来自路由懒加载的 Monaco 查询页；简单手工拆包不会减少该路由的总下载量，暂不做仅消除警告的拆分。
- 优化阶段完成：Element Plus 改按需注册与样式；入口 JS 从约 1.09 MB 降到 637 KB（gzip 359→216 KB），全局 CSS 从约 390 KB 降到 268 KB（gzip 52→35 KB）。G6 依赖图改为点击打开侧栏后加载，任务监控自身 chunk 约 24 KB；抽屉实际回归通过。
- 保留的大 chunk：G6 图引擎仅依赖图功能开启时加载；Monaco 仅进入数据库查询路由（约 2.69 MB，gzip 706 KB）。进一步拆解需考虑编辑器启动请求与交互性能，不为消除构建告警盲目切碎。
